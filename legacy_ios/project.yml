name: LegacyApp
options:
  bundleIdPrefix: com.example
  developmentLanguage: ja
  deploymentTarget:
    iOS: "16.0"
targets:
  LegacyApp:
    type: application
    platform: iOS
    sources:
      - path: LegacyApp/Sources
      - path: LegacyApp/Resources
    dependencies:
      - sdk: libsqlite3.tbd
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.legacyapp
        CLANG_ENABLE_OBJC_ARC: YES
        TARGETED_DEVICE_FAMILY: "1,2"
        GENERATE_INFOPLIST_FILE: YES
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: ""
        # Flutter統合の新規コード(ConfirmFlutterViewController)はSwiftで
        # 書いているため、既存のObjective-Cコードとの相互運用に必要な設定。
        SWIFT_VERSION: "5.0"
        SWIFT_OBJC_BRIDGING_HEADER: LegacyApp/Sources/LegacyApp-Bridging-Header.h
        ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES: YES
    info:
      path: Generated-Info.plist
      properties:
        CFBundleDisplayName: LegacyApp
        CFBundleLocalizations: [ja, en]
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations:
            UIWindowSceneSessionRoleApplication:
              - UISceneConfigurationName: Default Configuration
                UISceneDelegateClassName: SceneDelegate
schemes:
  LegacyApp:
    build:
      targets:
        LegacyApp: all
    run:
      config: Debug
    archive:
      config: Release
